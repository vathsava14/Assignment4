@{
    ViewData["Title"] = "Explore";
}

@model Assignment4.Models.SectorTotals

<div class="container bg-white">
    <div class="row justify-content-center pt-3">
        <div class="col-9 pb-2 mt-4 mb-2 border-bottom">
            <h2 class="text-center">Exploring the Population of Educated People by County</h2>
        </div>
    </div>
    <div class="row justify-content-center pt-3 pb-1">
        <div class="col text-center">
            <a asp-controller="Create" asp-action="Index">Create New Record</a>
        </div>
    </div>
    <br />
    <div class="row justify-content-center">
        <div class="col-auto">
            <table class="table table-responsive">
                <thead>
                    <tr>
                        <th class="align-middle" scope="col">County Name</th>
                        <th class="align-middle" scope="col">Total Population</th>
                        <th class="align-middle" scope="col">Population with Bachelor's Degree</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var row in Model.data)
                    {
                        <tr>
                            <td>
                                @row.Sector
                            </td>
                            <td>
                                @row.Value
                            </td>
                            <td>
                                @row.Value
                            </td>
                            <td>
                                <form asp-controller="Changes" asp-action="Index" method="post">
                                    <input type="hidden" name="sector" value="@row.Sector" />
                                    <input type="hidden" name="source" value="@row.Source" />
                                    <button type="submit" class="btn btn-primary btn-sm"><b>Details</b></button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <br />
</div>
